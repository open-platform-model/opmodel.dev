---
title: Documentation
description: Everything about the Open Platform Model, from a first look to the full reference.
---

<!-- Planned page: the docs home, an explanation owned by the `opm` repository. It keeps this front matter and adds five headings, in this order. "In Kubernetes terms": one short paragraph with OPM as the subject: OPM describes an application as a module and renders it into Kubernetes objects through the catalogs the cluster's platform subscribes to; the nearest idea is Helm plus a GitOps controller, and the comparison stops at templates (a module has none) and at the platform (it, not the module, decides which objects a component becomes); point to "What OPM is". "How it works": how the documentation is organised, by what the reader is holding when they arrive, one line per section with its title as the site shows it: Start here (nothing yet: what OPM is, the quickstart, what it does not do), Concepts (a question about why), Authoring modules (a blank module file), Deploying and operating (a cluster: the CLI or the operator), Extending OPM (a missing trait, transformer or catalog), Embedding the kernel (a Go program), Reference (a field, command or resource name) and Diagnostics (an error message); each section's index lists its pages grouped as tutorials, how-to guides, explanations and reference. "Why it is built this way": readers who author modules, run clusters and extend catalogs rarely need each other's pages, so sections follow the reader's situation rather than page type or repository; Diagnostics sits at the top level because readers arrive from a pasted error message; reference that can be derived from source is generated from it, so a rename cannot leave it stale. "Common mistakes": starting in Reference to learn OPM (begin with "Quickstart"); assuming a Helm feature such as hooks or rollback has an OPM counterpart (read "What OPM does not do"); looking for secrets documentation, which is pending and not on the site. "What enforces this": the badge on every rule in Concepts and Reference, cue (CUE refuses it at unification), kernel (the render refuses it), publish (an `opm module publish` or `opm catalog publish` gate refuses it) and convention (nothing checks it), pointing to "What enforces a rule". Check against: enhancements/0018/02-design.md, enhancements/0018/03-decisions.md, opmodel.dev/site/content/docs/start/index.md, opmodel.dev/site/content/docs/concepts/index.md, opmodel.dev/site/content/docs/authoring/index.md, opmodel.dev/site/content/docs/operating/index.md, opmodel.dev/site/content/docs/extending/index.md, opmodel.dev/site/content/docs/embedding/index.md, opmodel.dev/site/content/docs/reference/index.md, opmodel.dev/site/content/docs/diagnostics/index.md -->
