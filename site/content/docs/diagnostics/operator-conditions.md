---
title: "Operator conditions"
description: "Every status condition the operator sets on an instance, and what each one means."
type: reference
draft: true
sidebar:
  order: 40
---

:::note[Draft]
Owned by the `opm-operator` repository.
:::

<!-- One sentence: this page lists every status condition type and reason the operator writes on its four resource kinds, ModuleInstance, ModulePackage, Platform and TransformerRegistration. Then state the convention once, in Flux's terms, since the operator reuses Flux's condition helpers: a stalled failure sets Ready=False and Stalled=True with the same reason; an in-progress or deferred state sets Reconciling=True and Ready=Unknown with the same reason; success removes both. Rows are ordered by kind, workload kinds first, and within a kind in reconcile order. Mention in one line that some reasons appear only on events, never on a condition: Applied, Pruned, Resumed, NoOp, RenderWarning, OrphanedOnDeletion; Generated is both. Verify: the description names only instances, while the table covers all four kinds; the author decides whether to widen the description or split the page. Check against: opm-operator/internal/status/conditions.go, opm-operator/api/v1alpha1/conditions.go -->

| Condition and reason | Resource | Status | When the operator sets it |
| --- | --- | --- | --- |
| `Reconciling`: `Progressing` | <!-- ModuleInstance, ModulePackage --> | <!-- True, with Ready=Unknown --> | <!-- Start of every reconcile, message "Reconciliation in progress". "Progressing" is a literal in the reconcilers, not a named constant. Check against: opm-operator/internal/reconcile/moduleinstance.go, opm-operator/internal/reconcile/modulepackage.go --> |
| `Ready`: `ReconciliationSucceeded` | <!-- ModuleInstance, ModulePackage --> | <!-- True --> | <!-- Rendered, applied and, with `spec.prune`, pruned; message "Reconciliation succeeded". Check against: opm-operator/internal/reconcile/moduleinstance.go, opm-operator/internal/status/conditions.go --> |
| `Ready`: `Suspended` | <!-- ModuleInstance, ModulePackage --> | <!-- False; Reconciling and Stalled removed --> | <!-- `spec.suspend: true`; message "Reconciliation is suspended". A Normal event with reason Resumed marks the return. Check against: opm-operator/internal/status/conditions.go, opm-operator/internal/reconcile/modulepackage.go --> |
| `Ready`: `PlatformNotReady` | <!-- ModuleInstance, ModulePackage --> | <!-- False, not stalled --> | <!-- No generated platform package exists yet; nothing is applied or pruned, and the object retries on backoff and when the Platform is generated. Check against: opm-operator/internal/reconcile/moduleinstance.go, opm-operator/internal/render/kernel_module_renderer.go --> |
| `Ready`: `ResolutionFailed` | <!-- ModuleInstance, ModulePackage --> | <!-- False, Stalled=True --> | <!-- The render refused on unresolved demands or unmatched components, or module acquisition refused on an identity mismatch (ModuleInstance only); also untyped errors matched by message text, "synthesizing release" on a ModuleInstance and "loading package" or "resolving" on a ModulePackage. See the entries Unresolved demands, No matching transformer and Identity mismatch. Check against: opm-operator/internal/reconcile/resolution.go --> |
| `Ready`: `SkewRefused` | <!-- ModuleInstance, ModulePackage --> | <!-- False, Stalled=True --> | <!-- The Platform's `spec.skewPolicy` is Refuse and the module pins a newer OPM build than the platform; see the Version skew entry. Check against: opm-operator/internal/reconcile/resolution.go, opm-operator/api/v1alpha1/platform_types.go --> |
| `Ready`: `DuplicateIdentities` | <!-- ModuleInstance, ModulePackage --> | <!-- False, Stalled=True --> | <!-- Two rendered objects share one apiVersion, kind, namespace and name; the message names each identity and the components and transformers producing it; nothing is applied. Check against: opm-operator/internal/reconcile/resolution.go, library/opm/helper/objectset/objectset.go --> |
| `Ready`: `RenderFailed` | <!-- ModuleInstance, ModulePackage --> | <!-- False, Stalled=True --> | <!-- Any other render failure: a transform failure, an over-subscribed provider contract, `spec.values` that do not satisfy the module's #config ("validating values against the module's #config"), or the render digest could not be computed. Verify: a module that cannot be fetched from the registry ("acquiring module: ...") also lands here rather than under ResolutionFailed, because no typed error or matched text classifies it. See the entries Transform failed and Over-subscribed provider contracts. Check against: opm-operator/internal/reconcile/resolution.go, opm-operator/internal/reconcile/moduleinstance.go, opm-operator/internal/render/kernel_module_renderer.go --> |
| `Ready`: `ApplyFailed` | <!-- ModuleInstance, ModulePackage --> | <!-- False; Stalled=True only when rendered resources cannot be converted --> | <!-- Server-side apply failed and retries on backoff; on a ModuleInstance also when the operator could not decide whether an apply would drop a contract dependents need. Check against: opm-operator/internal/reconcile/moduleinstance.go, opm-operator/internal/reconcile/modulepackage.go --> |
| `Ready`: `ImpersonationFailed` | <!-- ModuleInstance, ModulePackage --> | <!-- False, Stalled=True --> | <!-- The client for `spec.serviceAccountName` (or the manager's default service account) could not be built, or apply or prune was forbidden under that identity; also during deletion. Check against: opm-operator/internal/reconcile/moduleinstance.go --> |
| `Ready`: `PruneFailed` | <!-- ModuleInstance, ModulePackage --> | <!-- False --> | <!-- `spec.prune` is true and deleting a stale inventory entry failed; retries. Link the explanation Deletion and pruning. Check against: opm-operator/internal/reconcile/moduleinstance.go --> |
| `Ready`: `DeletionSAMissing` | <!-- ModuleInstance, ModulePackage --> | <!-- False, Stalled=True --> | <!-- During deletion the impersonated service account no longer exists, so nothing can be cleaned up; the annotation `opm.dev/force-delete-orphan: "true"` orphans the inventory and releases the finalizer. Link the page Delete an instance safely. Check against: opm-operator/internal/reconcile/moduleinstance.go, opm-operator/api/v1alpha1/common_types.go --> |
| `Ready`: `ManagedExternally` | <!-- ModuleInstance --> | <!-- Unknown; Reconciling and Stalled removed --> | <!-- `spec.owner: cli`; the operator does not reconcile the instance; message "ModuleInstance is managed externally by the CLI". Link the explanation Who owns an instance. Check against: opm-operator/internal/status/conditions.go, opm-operator/api/v1alpha1/moduleinstance_types.go --> |
| `Ready`: `DependentsRemain` | <!-- ModuleInstance --> | <!-- False, not stalled --> | <!-- The instance provides a TransformerRegistration, and its new render drops a contract other instances still demand; that claim is withheld from apply, the rest applies, and the message names the claim, the contracts and the dependent count. Check against: opm-operator/internal/reconcile/moduleinstance.go, opm-operator/internal/status/conditions.go --> |
| `ModuleResolved`: `ModuleResolved` | <!-- ModuleInstance --> | <!-- True --> | <!-- The module was acquired and rendered; message "module resolved: <path>@<version>". Verify: the operator never sets it False, so a later failure leaves it True from the last success. Check against: opm-operator/internal/status/conditions.go, opm-operator/internal/reconcile/moduleinstance.go --> |
| `Drifted`: `DriftDetected` | <!-- ModuleInstance --> | <!-- True --> | <!-- Drift detection found live objects that differ from the render; message "<n> resource(s) drifted from desired state". Informational, never moves Ready; removed after a successful apply. Check against: opm-operator/internal/status/conditions.go, opm-operator/internal/reconcile/moduleinstance.go --> |
| `Ready`: `DependenciesNotReady` | <!-- ModulePackage --> | <!-- False --> | <!-- An object in `spec.dependsOn` is not Ready, or could not be read; message "waiting for dependency <name>". Check against: opm-operator/internal/reconcile/modulepackage.go, opm-operator/api/v1alpha1/modulepackage_types.go --> |
| `Ready`: `SourceNotReady` | <!-- ModulePackage --> | <!-- False; Stalled=True when the source object is missing or of an unsupported kind --> | <!-- The Flux source in `spec.sourceRef` has no ready artifact. Check against: opm-operator/internal/reconcile/modulepackage.go --> |
| `Ready`: `FetchFailed` | <!-- ModulePackage --> | <!-- False --> | <!-- Downloading or extracting the source artifact failed; retries. Check against: opm-operator/internal/reconcile/modulepackage.go --> |
| `Ready`: `PathNotFound` | <!-- ModulePackage --> | <!-- False, Stalled=True --> | <!-- `spec.path` does not exist in the artifact. Check against: opm-operator/internal/reconcile/modulepackage.go --> |
| `Ready`: `InstanceFileNotFound` | <!-- ModulePackage --> | <!-- False, Stalled=True --> | <!-- The directory at `spec.path` holds no `instance.cue`. Check against: opm-operator/internal/reconcile/modulepackage.go --> |
| `Ready`: `UnsupportedKind` | <!-- ModulePackage --> | <!-- False, Stalled=True --> | <!-- The package at `spec.path` is not a ModuleInstance package; only that kind renders. Check against: opm-operator/internal/reconcile/modulepackage.go, opm-operator/internal/render/kernel_package_renderer.go --> |
| `Ready`: `Generated` | <!-- Platform --> | <!-- True --> | <!-- The platform module was generated from `spec.registry` and the active claims, built, and recorded for renders; message "Platform module generated and built for generation <n>". Check against: opm-operator/internal/controller/platform_controller.go --> |
| `Ready`: `GenerateFailed` | <!-- Platform --> | <!-- False, Stalled=True --> | <!-- The platform module could not be generated or written to the operator's disk. Check against: opm-operator/internal/controller/platform_controller.go --> |
| `Ready`: `BuildFailed` | <!-- Platform --> | <!-- False, Stalled=True --> | <!-- A pinned catalog version does not resolve (unpublished pin or unreachable registry), the generated module does not build, its contract inventory cannot be read, or a stored `spec.registry` entry lacks its `version`. Check against: opm-operator/internal/controller/platform_controller.go --> |
| `Ready`: `OverSubscribedContracts` | <!-- Platform --> | <!-- False, Stalled=True --> | <!-- A provider-fulfilled contract is required by transformers from more than one enabled catalog; the package is not recorded, so renders keep the last good one. See the Over-subscribed provider contracts entry. Check against: opm-operator/internal/controller/platform_inventory.go --> |
| `Ready`: `ComparablePredicates` | <!-- Platform --> | <!-- False, Stalled=True --> | <!-- Two enabled transformers have comparable match predicates over a shared catalog-fulfilled contract, so both would render the same component; the message names each broader and narrower pair. Reported alone only when nothing is over-subscribed. Check against: opm-operator/internal/controller/platform_inventory.go --> |
| `ContractsFulfilled`: `ContractsFulfilled` | <!-- Platform --> | <!-- True --> | <!-- Every provider-fulfilled contract the enabled catalogs define has a provider; the message gives the defined count. Written only when a package is recorded or kept. Check against: opm-operator/internal/controller/platform_inventory.go --> |
| `ContractsFulfilled`: `NoContractsDefined` | <!-- Platform --> | <!-- True --> | <!-- The enabled catalogs define no contract, so nothing was verified; not a pass. Check against: opm-operator/internal/controller/platform_inventory.go --> |
| `ContractsFulfilled`: `UnfulfilledContracts` | <!-- Platform --> | <!-- False --> | <!-- Some provider-fulfilled contracts have no provider; a module demanding one will fail with an unresolved demand. Never moves Ready. Check against: opm-operator/internal/controller/platform_inventory.go --> |
| `Ready`: `Accepted` | <!-- TransformerRegistration --> | <!-- True --> | <!-- The claim passed every check; message "Claim accepted for catalog <catalog> at <version>". Accepted is not active. Check against: opm-operator/internal/controller/transformerregistration_controller.go --> |
| `Ready`: `PlatformNotReady` | <!-- TransformerRegistration --> | <!-- Unknown, Reconciling=True --> | <!-- No verdict yet: no platform has been generated, or its resolution or providers could not be read. Check against: opm-operator/internal/controller/transformerregistration_controller.go --> |
| `Ready`: `CatalogUnresolved` | <!-- TransformerRegistration --> | <!-- False, Stalled=True --> | <!-- `spec.catalog` at `spec.version` resolves to nothing: a registry or coordinate problem. Check against: opm-operator/internal/controller/transformerregistration_controller.go --> |
| `Ready`: `CatalogWrongKind` | <!-- TransformerRegistration --> | <!-- False, Stalled=True --> | <!-- The claimed artifact resolved and is not a catalog. Check against: opm-operator/internal/controller/transformerregistration_controller.go --> |
| `Ready`: `ProvidesMismatch` | <!-- TransformerRegistration --> | <!-- False, Stalled=True --> | <!-- `spec.provides` is not exactly the contract set re-derived from the catalog. Check against: opm-operator/internal/controller/transformerregistration_controller.go, opm-operator/internal/controller/transformerregistration_contracts.go --> |
| `Ready`: `ProviderInventoryPending` | <!-- TransformerRegistration --> | <!-- Unknown, Reconciling=True --> | <!-- The ModuleInstance named by `spec.providerRef` has not written an inventory yet; a race, not a verdict. Check against: opm-operator/internal/controller/transformerregistration_controller.go --> |
| `Ready`: `ProviderMismatch` | <!-- TransformerRegistration --> | <!-- False, Stalled=True --> | <!-- The claim did not come from the ModuleInstance its `spec.providerRef` names: the instance is absent, or its inventory settled without this claim. Check against: opm-operator/internal/controller/transformerregistration_controller.go --> |
| `Ready`: `BuildIncompatible` | <!-- TransformerRegistration --> | <!-- False, Stalled=True --> | <!-- The claimed catalog requires a shared OPM path at a version the platform did not resolve to, or its requirements could not be read. Check against: opm-operator/internal/controller/transformerregistration_buildcompat.go --> |
| `Ready`: `DuplicateClaim` | <!-- TransformerRegistration --> | <!-- False, Stalled=True; Unknown with Reconciling=True when competing claims could not be listed --> | <!-- Another claim already holds this provider catalog; the message names the holder. Check against: opm-operator/internal/controller/transformerregistration_controller.go --> |
| `Ready`: `ContractSubscribed` | <!-- TransformerRegistration --> | <!-- False, Stalled=True --> | <!-- A contract the claim provides is already provided by a catalog subscribed in the Platform's `spec.registry`; fix by disabling that subscription or withdrawing the claim. Check against: opm-operator/internal/controller/transformerregistration_controller.go --> |
| `Ready`: `ContractClaimed` | <!-- TransformerRegistration --> | <!-- False, Stalled=True; Unknown with Reconciling=True when competing providers could not be listed --> | <!-- Another active claim already provides one of the contracts; an accepted but inactive claim never causes this. Check against: opm-operator/internal/controller/transformerregistration_controller.go --> |
| `Ready`: `DependentsRemain` | <!-- TransformerRegistration --> | <!-- False, Stalled=True --> | <!-- Deletion is blocked because instances still demand contracts the claim provides; the message names them. The claim stays accepted and active until they are removed. Check against: opm-operator/internal/controller/transformerregistration_dependents.go --> |
| `Active`: `ProviderNotReady` | <!-- TransformerRegistration --> | <!-- False --> | <!-- Accepted, and the provider ModuleInstance has not reported Ready=True yet. Removed when the claim is later refused. Check against: opm-operator/internal/controller/transformerregistration_controller.go --> |
| `Active`: `ProviderReady` | <!-- TransformerRegistration --> | <!-- True, latched --> | <!-- The provider ModuleInstance reported Ready=True; the claim's catalog joins the platform. Never returns to False, even through a provider outage; only deletion ends it. Check against: opm-operator/internal/controller/transformerregistration_controller.go, opm-operator/docs/design/transformer-registration-activation.md --> |

## See also

<!-- By title: Operator resources (the four kinds and their spec fields), the diagnostics entries Unresolved demands, No matching transformer, Identity mismatch, Version skew, Transform failed and Over-subscribed provider contracts, the explanations Who owns an instance and Deletion and pruning, and the guide Delete an instance safely. Check against: opmodel.dev/site/content/docs/reference/operator-resources.md, opmodel.dev/site/content/docs/operating -->
