---
// Starlight renders LanguageSelect in the header and in the mobile menu, which
// is where the version switch belongs too, so the override adds it in front of
// the stock language select.
import Default from '@astrojs/starlight/components/LanguageSelect.astro'
import VersionSelect from './VersionSelect.astro'
---

<VersionSelect />
<Default />
